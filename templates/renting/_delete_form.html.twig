<form method="post" action="{{ path('renting_delete', {'id': renting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ renting.id) }}">
    <button class="btn">Delete</button>
</form>
